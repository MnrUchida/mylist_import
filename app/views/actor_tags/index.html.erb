<div class="container" >
  <h3>Actors</h3>

  <div id="actors">
    <% @actors.each do |actor| %>
      <div class="row py-3 border" id="<%= dom_id actor %>">
        <div class="col-6">
          <%= link_to actor.name, actor_path(actor), class: 'link-dark' %>
        </div>
        <div class="col-1">
          <%= actor.actor_tag_articles_count %>
        </div>
      </div>
    <% end %>
  </div>
  <div class="d-flex border" >
    <%= paginate @actors %>
  </div>
</div>
