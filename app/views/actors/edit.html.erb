<h1>Editing actor</h1>

<%= link_to @actor.name, @actor.url, target: :_blank %>

<%= render partial: 'form', locals: { actor: @actor } %>

<br>

<div>
  <%= link_to "Show this actor", @actor %> |
  <%= link_to "Back to actors", actors_path %>
</div>
